# Visual Studio Community 使用指南（配套 C++ 教程）

> 这份文档配合 `C++从零到一完整教程.md` 使用。
> 教程的正文代码两种编译器都能跑，**差异只在于"怎么建工程、怎么运行、怎么调试"**。
> 你只需要读这一份，就够跟着教程走完全程。

---

## 目录

- [第 1 部分 · 一次性配置（只做一次）](#第-1-部分--一次性配置只做一次)
- [第 2 部分 · 每次写练习的标准流程](#第-2-部分--每次写练习的标准流程)
- [第 3 部分 · 对照教程：哪些章节要换做法](#第-3-部分--对照教程哪些章节要换做法)
- [第 4 部分 · 五类必踩的坑](#第-4-部分--五类必踩的坑)
- [第 5 部分 · 调试器速查](#第-5-部分--调试器速查)
- [第 6 部分 · 常见报错速查](#第-6-部分--常见报错速查)
- [第 7 部分 · 什么时候该学命令行](#第-7-部分--什么时候该学命令行)

---

# 第 1 部分 · 一次性配置（只做一次）

## 1.1 确认安装正确

打开 Visual Studio Installer（开始菜单里搜），确认已安装：

```
☑ 使用 C++ 的桌面开发          ← 工作负载
    ☑ MSVC v143 - VS 2022 C++ x64/x86 生成工具
    ☑ Windows 11 SDK（或 Windows 10 SDK）
```

**如果没装**：点"修改"，勾上这两项，点"修改"按钮，等它下载安装完。

## 1.2 建立一个学习用文件夹

```
D:\cpp-learning\          ← 不要放在桌面/中文路径/Program Files
```

**规则：**

- ❌ 不要有中文：`D:\C++学习\` → 可能出各种诡异问题
- ❌ 不要有空格：`D:\My Code\` → 某些工具会出错
- ✅ `D:\cpp-learning\` 这样最好

**为什么要单独建文件夹？** Visual Studio 每个项目会在磁盘上生成一堆文件（`.sln`、`.vcxproj`、`x64\`、`.vs\`）。集中管理才不会把电脑搞乱。

## 1.3 准备一个"配置模板项目"（强烈推荐）

Visual Studio 每次新建项目都要重新设置 C++17 和 `/utf-8`，很烦。**做一次模板，以后复制就行。**

**步骤：**

1. 新建一个**空项目**，命名 `_Template`，位置 `D:\cpp-learning\`
2. 按 [2.2 节](#22-配置项目属性c17--utf-8) 设置 C++17 和 `/utf-8`
3. 添加一个空的 `main.cpp`
4. 关闭 Visual Studio
5. 去 `D:\cpp-learning\_Template\` 把**整个文件夹复制**一份，重命名为你这次要做的练习名，比如 `Ch03_Variables`
6. 双击新文件夹里的 `.sln` 打开，改代码，运行

**⚠️ 复制后要做一件事**：把 `.vs` 隐藏文件夹删掉（里面是旧项目的缓存），否则可能有奇怪问题。

> **为什么复制文件夹而不是"另存为"？** Visual Studio 的"另存为"只改 `.sln` 名字，`.vcxproj` 里的项目名还是旧的，容易搞混。直接复制文件夹最干净。

**如果嫌麻烦**，就每次新建项目然后手动设置。两种都行，但别跳过 C++17 设置。

## 1.4 建议的界面设置

**把"错误列表"固定住**（很重要，你会一直用它看编译错误）：

```
视图 → 错误列表         (Ctrl + \, E)
```

然后点击窗口标题栏的**图钉图标**固定它。

**把错误列表筛选改成只看"生成"：**

错误列表窗口左上角有个下拉框，默认是"生成 + IntelliSense"。**改成"生成"**。

> **为什么？** IntelliSense 是编辑器的实时猜测，经常误报"未定义标识符"之类的错误（尤其是头文件刚加入项目时）。**只有"生成"里的错误才是真的编译器错误。**

---

# 第 2 部分 · 每次写练习的标准流程

## 2.1 完整流程（建议背下来）

```
1. 打开 Visual Studio
2. 创建新项目 → 空项目 (Empty Project)  ← 不是"控制台应用"！
3. 项目名称：英文，如 Ch05_Input
   位置：D:\cpp-learning
4. 右键"源文件" → 添加 → 新建项 → C++ 文件(.cpp) → 命名 main.cpp
5. 设置 C++17 + /utf-8（见 2.2）
6. 粘贴/敲代码
7. Ctrl + F5 运行
```

## 2.2 配置项目属性（C++17 + /utf-8）

**每个新项目都要做一遍。**

1. **确认"解决方案资源管理器"是打开的**（`视图 → 解决方案资源管理器`，`Ctrl + Alt + L`）
2. **右键项目名**（在解决方案下面的那个，**不是**最上面的"解决方案"），选 **属性**
   - 快捷键：选中项目后按 `Alt + Enter`
3. 左上角配置选 **所有配置**（这样 Debug 和 Release 都生效）

**设置 A：C++17 标准**

```
配置属性 → C/C++ → 语言 → C++ 语言标准 → ISO C++17 标准 (/std:c++17)
```

**设置 B：UTF-8 编码（解决中文乱码）**

```
配置属性 → C/C++ → 命令行 → 其他选项 → 填入  /utf-8
```

**设置 C（强烈推荐）：提高警告等级**

```
配置属性 → C/C++ → 常规 → 警告等级 → 等级 4 (/W4)
```

4. 点**确定**

> **⚠️ 如果左侧找不到 "C/C++" 这一项**：说明项目里还没有 `.cpp` 文件。先去第 4 步添加一个 `main.cpp`，再回来打开属性页。

> **⚠️ 关于编码的补充**：如果加了 `/utf-8` 反而出现乱码，说明源文件带 BOM。
> 解决：`文件 → 高级保存选项`（如果没有这一项，去 `工具 → 自定义 → 命令` 里添加），
> 把编码改成 **"UTF-8 无签名"**。
> 或者：加 `/utf-8` 的同时用记事本打开 `.cpp` 另存为 UTF-8（无 BOM）。

## 2.3 添加代码文件

**只能通过 Visual Studio 添加**，在文件夹里手动建文件不会被编译。

```
右键"源文件" → 添加 → 新建项 (Ctrl + Shift + A) → C++ 文件(.cpp) → 名字.cpp → 添加
```

**验证**：在"解决方案资源管理器"里能看到这个文件 = 成功。

**添加头文件：**

```
右键"头文件" → 添加 → 新建项 → 头文件(.h) → 名字.h → 添加
```

**添加已有的文件：**

```
右键"源文件" → 添加 → 现有项 (Shift + Alt + A) → 选择文件
```

## 2.4 运行

| 快捷键 | 什么时候用 | 行为 |
|---|---|---|
| **`Ctrl + F5`** | **99% 的情况** | 运行完停住显示"按任意键继续" |
| `F5` | 需要调试时 | 运行完立即关窗口 |
| `Ctrl + Shift + B` | 只检查能不能编译 | 不运行 |

**⭐ 新手只用记住 `Ctrl + F5`。**

## 2.5 生成的文件都在哪

以项目 `Ch05_Input` 为例，位于 `D:\cpp-learning\Ch05_Input\`：

```
Ch05_Input\
├── Ch05_Input.sln           ← 解决方案文件（双击这个打开）
├── Ch05_Input\
│   ├── Ch05_Input.vcxproj   ← 项目文件（配置都存在这里）
│   ├── Ch05_Input.vcxproj.filters
│   ├── main.cpp             ← 你的代码
│   ├── x64\
│   │   └── Debug\
│   │       ├── Ch05_Input.exe   ← 生成的可执行文件
│   │       └── Ch05_Input.pdb   ← 调试信息
│   └── x64\
│       └── Release\
│           └── Ch05_Input.exe
└── .vs\                     ← 缓存，可以随便删（删了会重新生成）
```

**⭐ 工作目录**：按 `Ctrl + F5` 时，程序的工作目录是 **`.vcxproj` 所在目录**，也就是 `D:\cpp-learning\Ch05_Input\Ch05_Input\`。

**这意味着**：第 25、29 章的文件读写练习，生成的 `.txt` / `.csv` 文件会出现在那里，**不是** `x64\Debug\`。

---

# 第 3 部分 · 对照教程：哪些章节要换做法

## 3.1 需要改做法的章节

| 教程位置 | 内容 | 你的做法 |
|---|---|---|
| **第 1 章 1.2 / 1.3 节** | 安装 MinGW / VS Code | **跳过**。你已经装好 VS 了 |
| **第 1 章 1.4 节** | g++ 命令行的解释 | 看一眼理解原理即可，不用敲 |
| **第 26 章 编译命令** | `g++ main.cpp utils.cpp -o app` | 见下面 [3.3 多文件项目](#33-第-26-章多文件项目) |
| **第 28 章 28.2 节** | g++ 编译选项 | 看教程 28.2 节末尾的"用 Visual Studio 的读者看这里"表格 |
| **第 28 章 28.4 节** | gdb 命令 | **跳过**，改用 VS 图形调试器（见本文档[第 5 部分](#第-5-部分--调试器速查)） |
| **第 28 章 28.5 节** | `-fsanitize=address,undefined` | 用 `/fsanitize=address`（只支持 address 部分） |
| **第 28 章 28.9 节** | Makefile | 可以跳过（VS 自带构建系统） |
| **第 28 章 28.10 节** | CMake | 可以跳过，但建议以后学（跨平台需要） |
| **第 29、30 章** | 项目编译 | 建空项目 → 加 `todo.cpp` / `matrix.cpp` → `Ctrl + F5` |

## 3.2 完全不用改的章节

**第 2–25、27 章的所有代码，以及第 29、30 章的项目代码，都可以直接复制到 Visual Studio 里运行**（只要做了 C++17 + `/utf-8` 设置）。

教程里所有 `g++ -std=c++17 ... -o app` 形式的命令，对应到 VS 就是：

```
Ctrl + F5
```

就这么简单。

## 3.3 第 26 章：多文件项目

教程里说：

```bash
g++ -std=c++17 -Wall -Iinclude src/main.cpp src/shapes/circle.cpp -o build/app
```

**Visual Studio 的做法：**

1. 建空项目
2. 创建一个叫 `include` 的筛选器（右键项目 → 添加 → 新建筛选器），或者干脆不用筛选器
3. **把所有 `.h` 和 `.cpp` 都通过"添加 → 新建项/现有项"加进项目**
4. **不需要任何 `-I` 配置** —— Visual Studio 自动把项目目录加入头文件搜索路径

**建议新手用的文件结构（平铺）：**

```
项目名\
├── main.cpp
├── math_utils.h
├── math_utils.cpp
├── string_utils.h
└── string_utils.cpp
```

然后 `#include "math_utils.h"` 就能用。

**教程里带目录的写法**（`#include "shapes/circle.h"`）要在 VS 里跑，需要：

```
项目属性 → C/C++ → 常规 → 附加包含目录 → 加上  $(ProjectDir)include
```

> **建议**：先用平铺结构把第 26 章的原理（声明放 `.h`、定义放 `.cpp`、头文件保护）搞明白，再折腾目录结构。

---

# 第 4 部分 · 五类必踩的坑

## 坑 1：`M_PI` 未定义

**症状：**

```
error C2065: "M_PI": 未声明的标识符
```

**原因**：`M_PI` 是 GCC 扩展，MSVC 上没有。

**解决**：本教程的代码已经全部改用 `constexpr double kPi = 3.14159265358979323846;`，不会遇到。

如果你从别处抄了用 `M_PI` 的代码：

```cpp
// 方案 1（推荐）
#ifndef M_PI
constexpr double M_PI = 3.14159265358979323846;
#endif

// 方案 2（必须在 #include <cmath> 之前）
#define _USE_MATH_DEFINES
#include <cmath>
```

## 坑 2：中文乱码

**症状：**

```
鎴戝彨灏忔槑              ← 本该是"我叫小明"
```

**原因**：源文件是 UTF-8，MSVC 默认按 GBK 读。

**解决**：

```
项目属性 → C/C++ → 命令行 → 其他选项 → 加  /utf-8
```

**如果加了还乱码**，说明文件带 BOM。用 `文件 → 高级保存选项` 改成"UTF-8 无签名"。

**如果输入中文也乱码**，在 `main` 开头加：

```cpp
#include <windows.h>

int main() {
    SetConsoleCP(65001);
    SetConsoleOutputCP(65001);
    // ...
}
```

**注意**：**只写中文注释不会乱码**（注释在编译时被丢掉）。乱码只影响**中文字符串字面量**。

## 坑 3：`main` 重复定义

**症状：**

```
error LNK2005: main 已经在 main.obj 中定义
fatal error LNK1169: 找到一个或多个多重定义的符号
```

**原因**：一个项目里有多个 `main` 函数。

**解决**（选一个）：

| 方法 | 做法 |
|---|---|
| **推荐** | 一个练习一个项目。用 1.3 节的模板项目复制法最省事 |
| 快速 | 把旧代码的 `main` 改名为 `old_main`，或整段 `/* */` 注释掉 |
| 保留文件 | 右键多余的文件 → **移除**（不是删除），它就不参与编译了 |

## 坑 4：文件加了但没编译

**症状**：

- 改了代码，运行结果没变
- 报 `找不到 main` 或 `LNK2019: 无法解析的外部符号`

**原因**：文件在磁盘上，但没有"添加到项目"。

**解决**：

- 检查"解决方案资源管理器"里有没有这个文件
- 没有 → 右键"源文件" → 添加 → 现有项
- 有的文件显示为灰色/有个小红叉 → 说明它被"排除"了，右键 → 从项目中排除（取消勾选）

## 坑 5：`pch.h` 预编译头

**症状：**

```
error C1010: 在查找预编译头时遇到意外的文件结尾。
是否忘记了向源中添加 "#include "pch.h""?
```

**原因**：你建的是"控制台应用"而不是"空项目"，项目要求每个 `.cpp` 第一行加 `#include "pch.h"`。

**解决**（选一个）：

| 方法 | 做法 |
|---|---|
| **最好** | 删掉项目，重新建一个**空项目** |
| 快速 | 在每个 `.cpp` 最顶部加 `#include "pch.h"` |
| 一劳永逸 | 项目属性 → C/C++ → 预编译头 → 预编译头 = **不使用预编译头** |

---

# 第 5 部分 · 调试器速查

Visual Studio 的调试器是它最大的优势，**比命令行 gdb 好用太多**。一定要学会。

## 5.1 核心操作

| 操作 | 快捷键 | 说明 |
|---|---|---|
| 设置/取消断点 | **`F9`** | 光标所在行，左边出现**红点** |
| 开始调试 | **`F5`** | 运行到第一个断点停下 |
| 停止调试 | **`Shift + F5`** | |
| 单步（不进入函数） | **`F10`** | 最常用 |
| 单步（进入函数） | **`F11`** | 想进去看函数内部时用 |
| 跳出当前函数 | **`Shift + F11`** | |
| 继续到下一个断点 | **`F5`** | |
| 运行到光标处 | **`Ctrl + F10`** | 快速跳过一段代码 |

## 5.2 看变量

**方法 1：鼠标悬停（最快）**

鼠标移到变量上，会弹出一个小窗显示它的值。对 `std::vector` / `std::string` 还会显示可展开的树状结构。

**方法 2：局部变量窗口**

```
调试 → 窗口 → 局部变量  (Ctrl + Alt + V, L)
```

显示当前函数的所有局部变量。

**方法 3：监视窗口（自己指定要看什么）**

```
调试 → 窗口 → 监视 → 监视 1  (Ctrl + Alt + W, 1)
```

在表格里输入表达式，比如：

```
v.size()
v[0]
*ptr
matrix(1, 2)
```

**方法 4：自动窗口**

```
调试 → 窗口 → 自动  (Ctrl + Alt + V, A)
```

自动显示当前行和上一行用到的变量。

## 5.3 看崩溃位置

程序崩溃时（不是异常，是真的崩溃）：

1. Visual Studio 会**弹窗**，点"**中断**"
2. 会自动跳到出错的那一行（黄色高亮）
3. 打开 **`调试 → 窗口 → 调用堆栈`**（`Ctrl + Alt + C`）
4. 双击堆栈里的行，可以跳到上层调用的位置

**常见崩溃原因：**

| 现象 | 大概率原因 |
|---|---|
| `0xC0000005: 读取访问权限冲突` | 空指针/野指针解引用、数组越界 |
| 停在 `delete` 上 | 重复释放、释放非 new 的内存 |
| 堆栈溢出 | 无限递归 |

## 5.4 用 AddressSanitizer 抓越界（比调试器还强）

**用调试器查越界很痛苦（越界可能不立刻崩溃）。用 ASan 一秒定位。**

**开启：**

```
项目属性 → C/C++ → 常规 → 启用地址擦除器 → 是 (/fsanitize=address)
```

然后 `Ctrl + F5` 运行。越界时"输出"窗口会打印：

```
==12345==ERROR: AddressSanitizer: stack-buffer-overflow on address 0x...
READ of size 4 at ...
    #0 ... in main D:\cpp-learning\Ch10\main.cpp:6
```

**精确到行号。**

**注意：**

- MSVC 的 ASan **只支持 `/fsanitize=address`**，没有 GCC 那个 `undefined` 部分
- 抓不到整数溢出、除零等问题
- **发布前记得关掉**（会拖慢程序）

## 5.5 条件断点

**在第 1000 次循环时才想看**：右键红点 → `条件` → 输入 `i == 1000`。

适合调循环里的 bug。

## 5.6 一个完整的调试练习

用第 28 章的示例（`findMax` 函数），跟着做一遍：

```cpp
#include <iostream>
#include <vector>

int findMax(const std::vector<int>& v) {
    int maxVal = v[0];                                  // ← 在这行按 F9 设断点
    for (std::size_t i = 0; i < v.size(); i++) {
        if (v[i] > maxVal) {                            // ← 也在这里设断点
            maxVal = v[i];
        }
    }
    return maxVal;
}

int main() {
    std::vector<int> data = {3, 7, 2, 9, 1};
    int result = findMax(data);
    std::cout << "最大值: " << result << "\n";
    return 0;
}
```

**操作步骤：**

1. 在第 5 行（`int maxVal = v[0];`）按 `F9`，左边出现红点
2. 按 `F5`，程序停在断点，当前行变成黄色高亮
3. 把鼠标移到 `v` 上 → 展开看到 `[0]=3, [1]=7, [2]=2, [3]=9, [4]=1`
4. 在监视窗口输入 `v.size()` → 显示 `5`
5. 按 `F10` 执行 `int maxVal = v[0];`，此时悬浮 `maxVal` → `3`
6. 按 `F10` 进入循环，观察 `i` 的变化
7. 按 `F5` 继续，看它停在循环内的断点上，反复看 `maxVal` 怎么被更新
8. 按 `Shift + F5` 停止

**学会这个流程，你就能自己查绝大多数 bug 了。**

---

# 第 6 部分 · 常见报错速查

## 6.1 MSVC 错误码对照

| 错误码 | 含义 | 排查 |
|---|---|---|
| `C2065` | 未声明的标识符 | 忘了 `#include`、拼错、少 `std::` |
| `C2039` | 不是某类的成员 | 成员名拼错、访问私有成员 |
| `C2143` | 语法错误 | 常是**上一行漏了分号** |
| `C2447` | 函数定义语法错误 | 括号不配对、漏分号 |
| `C3861` | 找不到标识符 | 函数名拼错、没包含头文件 |
| `C2664` | 参数类型不匹配 | 检查实参类型 |
| `C2440` | 无法从 X 转换为 Y | 需要显式转换 `static_cast` |
| `C2784`/`C2780` | 模板参数推导失败 | 参数类型和模板不匹配 |
| `C4996` | 用了不安全的 C 函数 | 见 6.3 |
| `C1010` | 忘了 `#include "pch.h"` | 见[坑 5](#坑-5pchh-预编译头) |
| `C1083` | 无法打开包含文件 | 头文件路径不对 / 文件名拼错 |
| `LNK2019` | 无法解析的外部符号 | = g++ 的 `undefined reference`，见 6.2 |
| `LNK2005` | 重复定义 | 多个 `main`，或头文件里放了非 inline 定义 |
| `LNK1169` | 多重定义的符号 | 同上 |
| `LNK1104` | 无法打开文件 | 前一次运行的程序还在运行，先关掉控制台窗口 |

## 6.2 `LNK2019` 排查清单

**这是第二常见的错误**（第一是漏分号）。

```
error LNK2019: 无法解析的外部符号 "int __cdecl add(int,int)" (?add@@YAHHH@Z)
```

**原因排查（按顺序）：**

1. **函数声明了但没写实现**
2. **实现所在的 `.cpp` 没加进项目** → 检查"解决方案资源管理器"
3. **`main` 函数名拼错**：`mian`、`Main`、`MAIN` 都是错的。必须是 `main`
4. **函数签名不一致**：`.h` 里是 `int add(int, int)`，`.cpp` 里写成 `int add(int a, int b) const`（多了 const）或参数类型不同
5. **命名空间不匹配**：头文件里在 `namespace math {}` 里声明，`.cpp` 里的定义忘了包在同一个命名空间
6. **模板定义放在了 `.cpp` 里**（第 26.7 节的经典问题）→ 把定义移到 `.h`

## 6.3 `C4996` 不安全函数警告

```
error C4996: 'strcpy': This function or variable may be unsafe.
Consider using strcpy_s instead.
```

**解决：**

| 方法 | 做法 | 评价 |
|---|---|---|
| **最好** | 改用 `std::string`，不用 C 字符串 | ✅ 本教程推荐 |
| 可以 | 改用 `strcpy_s`、`sprintf_s` 等 `_s` 版本 | 但代码不可移植 |
| 快速 | 文件第一行加 `#define _CRT_SECURE_NO_WARNINGS` | 治标 |
| 全局 | 项目属性 → C/C++ → 预处理器 → 预处理器定义 → 加 `_CRT_SECURE_NO_WARNINGS` | 治标 |

## 6.4 程序一闪而过

**原因**：你按的是 `F5`（调试），不是 `Ctrl + F5`（不调试）。

**解决**：按 `Ctrl + F5`。

**如果还是闪退**，说明程序崩溃了。查看：

1. 按 `F5` 调试运行，崩溃时会停在出错位置
2. 检查数组越界、空指针

## 6.5 编译很慢

| 情况 | 说明 |
|---|---|
| 第一次编译慢（10-30 秒） | 正常，要编译标准库头文件 |
| 后续编译快（1-3 秒） | 正常，增量编译 |
| 每次都慢 | 开启多核编译：项目属性 → C/C++ → 常规 → **多处理器编译 = 是 (`/MP`)** |
| Debug 比 Release 慢 | 正常，Debug 不优化 |

## 6.6 IntelliSense 报错但能编译

**症状**：代码下面有红线，"错误列表"里有错误，但 `Ctrl + F5` 能正常跑。

**原因**：IntelliSense（编辑器）和编译器是两套引擎，偶尔不一致。

**解决：**

1. 把"错误列表"的筛选改成 **"生成"**（只看编译器真实输出）
2. 如果还是很烦：右键项目 → `重新扫描解决方案`，或删除 `.vs` 文件夹重开

---

# 第 7 部分 · 什么时候该学命令行

## 7.1 现在可以完全不用命令行

按这份指南，你可以用 Visual Studio 走完整本教程。**不需要装 MinGW，不需要敲任何 `g++` 命令。**

## 7.2 但下面这些时候你需要它

| 场景 | 为什么 |
|---|---|
| 想用 GitHub 上的开源库 | 很多库只提供 CMake / Makefile 构建 |
| 想参与开源项目 | CI/CD 都是命令行 |
| 想部署到 Linux 服务器 | Windows 编译的 exe 在 Linux 上跑不了 |
| 想用 CI（GitHub Actions 等） | 全自动构建 |
| 面试 | 手写代码环境常是命令行 |
| 想理解"编译到底发生了什么" | 命令行最直观 |

## 7.3 最省事的过渡方案

**Visual Studio 自带命令行工具**，不用额外装东西：

```
开始菜单 → 搜索 "x64 Native Tools Command Prompt for VS 2022" → 打开
```

在这个终端里就可以用 `cl.exe`：

```bat
cd /d D:\cpp-learning\test
cl /std:c++17 /W4 /EHsc /utf-8 main.cpp /Fe:app.exe
app.exe
```

**MSVC 命令行选项对照**（详见教程 28.2 节的表格）：

| MSVC | g++ | 含义 |
|---|---|---|
| `/std:c++17` | `-std=c++17` | 标准 |
| `/W4` | `-Wall -Wextra` | 警告等级 |
| `/WX` | `-Werror` | 警告当错误 |
| `/EHsc` | — | 异常模型（**几乎总要加**） |
| `/Zi` | `-g` | 调试信息 |
| `/O2` | `-O2` | 优化 |
| `/Od` | `-O0` | 不优化 |
| `/I<dir>` | `-I<dir>` | 包含目录 |
| `/D<名字>` | `-D<名字>` | 定义宏 |
| `/c` | `-c` | 只编译不链接 |
| `/Fe:app.exe` | `-o app` | 输出文件名 |
| `/fsanitize=address` | `-fsanitize=address` | ASan |

## 7.4 建议的学习时机

**先把教程的第 1–25 章用 Visual Studio 走完。** 等你：

- 能熟练用 `Ctrl + F5` 运行、`F9`/`F10` 调试
- 理解了头文件、声明/定义、多文件项目

**再去学命令行和 CMake**（教程第 28 章）。那时候你会觉得命令行很自然，因为它只是把你在 VS 里点的那些属性变成了文字。

**不要一开始就学命令行** —— 同时学 C++ 语法、Visual Studio、命令行编译，认知负担太大。

---

# 附录 · Visual Studio 快捷键一览

## 最常用的 10 个

| 快捷键 | 作用 |
|---|---|
| `Ctrl + F5` | 编译并运行（不调试）← **最重要** |
| `F5` | 调试运行 |
| `F9` | 切换断点 |
| `F10` | 单步（不进入函数） |
| `F11` | 单步（进入函数） |
| `Shift + F5` | 停止调试 |
| `Ctrl + Shift + B` | 只编译 |
| `Ctrl + K, Ctrl + D` | 格式化代码（自动缩进） |
| `Ctrl + K, Ctrl + C` | 注释选中行 |
| `Ctrl + K, Ctrl + U` | 取消注释 |

## 编辑

| 快捷键 | 作用 |
|---|---|
| `Ctrl + Space` | 强制显示智能提示 |
| `Ctrl + .` | 快速修复（显示"灯泡"建议） |
| `Ctrl + K, Ctrl + F` | 格式化选中内容 |
| `Ctrl + D` | 复制当前行 |
| `Ctrl + X` / `Ctrl + C` / `Ctrl + V` | 剪切/复制/粘贴（不选中时整行操作） |
| `Ctrl + Up/Down` | 滚动但不移动光标 |
| `Alt + 上下拖动` | 多行同时编辑（列选择） |
| `Ctrl + Shift + V` | 剪贴板历史 |
| `Ctrl + G` | 跳到指定行 |
| `Ctrl + F` / `Ctrl + H` | 查找 / 替换 |
| `Ctrl + Shift + F` | 在整个解决方案中查找 |
| `F12` | 转到定义 |
| `Ctrl + F12` | 转到实现 |
| `Alt + F12` | 速览定义（不离开当前文件） |
| `Ctrl + M, Ctrl + O` | 折叠所有方法 |
| `Ctrl + M, Ctrl + L` | 展开全部 |
| `Ctrl + ]` | 跳到匹配的括号 |
| `Ctrl + Shift + Space` | 显示参数信息 |

## 窗口

| 快捷键 | 作用 |
|---|---|
| `Ctrl + Alt + L` | 解决方案资源管理器 |
| `Ctrl + \, E` | 错误列表 |
| `Ctrl + Alt + O` | 输出窗口 |
| `Ctrl + Alt + V, L` | 局部变量（调试时） |
| `Ctrl + Alt + W, 1` | 监视 1（调试时） |
| `Ctrl + Alt + C` | 调用堆栈（调试时） |
| `Alt + Enter` | 打开项目属性（选中项目时） |

## 项目

| 快捷键 | 作用 |
|---|---|
| `Ctrl + Shift + N` | 新建项目 |
| `Ctrl + Shift + A` | 添加新项 |
| `Shift + Alt + A` | 添加现有项 |
| `Ctrl + Shift + S` | 全部保存（建议经常按） |
| `Ctrl + Shift + B` | 生成解决方案 |
| `Ctrl + Alt + F7` | 重新生成解决方案 |

---

# 一句话总结

> **建空项目 → 加 `main.cpp` → 设 C++17 和 `/utf-8` → 代码 → `Ctrl + F5` → 出错按 `F9` + `F5` 调试。**

把这条流程练熟，教程剩下的 29 章你就能顺畅走完了。

---

*本文档配合 `C++从零到一完整教程.md` 使用。教程本身已针对 Visual Studio 做了适配（`M_PI` 已替换、第 1 章和第 28 章含 VS 专属说明）。*
